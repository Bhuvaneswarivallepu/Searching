#Linear Search
def linear_search(arr,target):
    for i in range(len(arr)):
        if(arr[i]==target):
            print("Element Found in the index:",i)
            return
    print("Not Found in index")
arr=[10,20,30,40,50]
target=int(input("Enter the Target value:"))
linear_search(arr,target)
